import React from 'react';

const store = () => {
	const [ state, setState ] = useState([]);
	const dispatch = () => {
		if ('GET_DESCRIPTORS') {
			return state;
		}
		if (('ADD_EMPLOYEE', emploee)) {
			setstate(setState([ ...state, emploee ]));
			return state;
		} else return state;
	};

	return <div />;
};
export default store;
